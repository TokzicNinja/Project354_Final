@model IEnumerable<Project_Final_354.GameGenre>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Home/Index.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GameGenre1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GameGenre1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameGenreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GameGenreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameGenreID })
        </td>
    </tr>
}

</table>
